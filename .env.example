HUB_HOST=0.0.0.0
HUB_PORT=8000
HUB_DB_PATH=/data/hub.db
HUB_LOG_LEVEL=INFO
HUB_UI_DIR=/app/ui
# Host dir holding one checkout per project (project = dir name); mounted read-only at /repos for the spec viewer
HUB_REPOS_HOST_DIR=/home/mdv/code
